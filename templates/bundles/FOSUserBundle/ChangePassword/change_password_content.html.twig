{% trans_default_domain 'FOSUserBundle' %}

<div class="well">
    {{ form_start(form, { 'action': path('fos_user_change_password') }) }}
        {{ form_widget(form) }}
        <div class="form-group">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <button type="submit" class="btn-default btn">{{ 'change_password.submit'|trans }}</button>
            </div>
        </div>
    {{ form_end(form) }}
</div>
