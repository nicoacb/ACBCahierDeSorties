{% extends "::layout.html.twig" %}

{% block title %}
  Ajouter une saison - {{ parent() }}
{% endblock %}

{% block body %}

  <h2>Ajouter une saison</h2>

  {{ include("AvironSortieBundle:Saison:form.html.twig") }}

  <p>
    <a href="{{ path('aviron_sortie_saison_liste') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
  </p>

{% endblock %}